# Handoff: Migrate Avatar Storage → Supabase Storage
_Paused: 2026-04-16T19:33:55Z_

<current_state>
All code changes are complete. We're in the final step: verifying the .env credentials are correct and that an avatar upload works end-to-end.

Last failure: "request signature mismatch" — caused by a truncated secret key. User regenerated keys.
New Access Key ID: 32f5704d7a295c94bfaab7b0cf25b63b
</current_state>

<completed_work>

- ✓ `src/lib/storage/minio.ts` → renamed to `src/lib/storage/supabase.ts`
  - Removed auto-bucket-create logic (R2/Supabase buckets are created in dashboard)
  - Updated credentials to use `SUPABASE_S3_ACCESS_KEY_ID` / `SUPABASE_S3_SECRET_ACCESS_KEY`
- ✓ `src/lib/storage/avatar.ts` — all env vars switched to `SUPABASE_*`, public URL now includes bucket name: `${AVATAR_PUBLIC_BASE}/${AVATAR_BUCKET}/${key}`
- ✓ `src/lib/server/user.ts` — import updated to `storage/supabase`
- ✓ `src/db/schema.ts` — comment updated
- ✓ `.env.example` — full `SUPABASE_*` var set documented
- ✓ `README.md` — MinIO section removed, Supabase setup added
- ✓ `docker/minio/` directory deleted
- ✓ `docker:up` / `docker:down` scripts removed from `package.json`
- ✓ User created Supabase project, `avatars` bucket (public), S3 access keys
</completed_work>

<remaining_work>

- [ ] Verify `.env` has complete (non-truncated) `SUPABASE_S3_SECRET_ACCESS_KEY`
- [ ] Restart `pnpm dev` and test avatar upload on `/profile` page
</remaining_work>

<decisions_made>

- **Supabase over Cloudflare R2**: R2 requires credit card; Supabase free tier does not
- **Kept `@aws-sdk/client-s3`**: Supabase Storage is S3-compatible — no SDK change needed
- **Public URL includes bucket name**: `<base>/<bucket>/<key>` — unlike R2 which had no bucket in URL
- **No migration**: User confirmed no existing avatars worth migrating, start fresh
</decisions_made>

<blockers>
- Secret key truncation: previous key was incomplete from screenshot copy. New keys generated — must ensure full secret is in `.env`.
</blockers>

## Required Reading (in order)
1. `src/lib/storage/supabase.ts` — the S3 client setup
2. `src/lib/storage/avatar.ts` — env var names and URL building logic
3. `.env.example` — full list of required vars

## Infrastructure State
- Supabase project ref: `uyplkgcwahwoofxlpaur`
- Region: `ap-south-1`
- Bucket: `avatars` (public)
- S3 Endpoint: `https://uyplkgcwahwoofxlpaur.storage.supabase.co/storage/v1/s3`
- Public URL base: `https://uyplkgcwahwoofxlpaur.storage.supabase.co/storage/v1/object/public`

<context>
Simple env-var credential issue. All the hard work (code migration) is done. Just need the correct secret key in .env and a successful test upload.
</context>

<next_action>
1. Open `.env` and confirm `SUPABASE_S3_SECRET_ACCESS_KEY` is the full value (64+ hex chars, not ending in `...`)
2. Restart `pnpm dev`
3. Go to `/profile`, upload an avatar image
4. If it works, commit all changes
</next_action>
